package com.me.models;

public class Editor extends User {
    public Editor(int id, String username, String password, String firstName, String lastName) {
        super(id, username, password, firstName, lastName);
    }
}
